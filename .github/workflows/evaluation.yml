name: Evaluation

on:
  push:
    branches: [ main, feature/* ]
  pull_request:
    branches: [ main ]

jobs:
  evaluation:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install ffmpeg (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install ffmpeg (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          brew update
          brew install ffmpeg

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Build
        run: |
          mkdir -p build
          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            cmake -S . -B build -DCMAKE_OSX_ARCHITECTURES=arm64 2>&1 | tee build/cmake_config.log
          else
            cmake -S . -B build 2>&1 | tee build/cmake_config.log
          fi
          cmake --build build --config Release 2>&1 | tee build/build.log

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ matrix.os }}
          path: |
            build/cmake_config.log
            build/build.log
            build/**/Testing/**

      - name: Run evaluation
        if: success()
        run: |
          python evaluation/run_evaluation.py

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: evaluation-results-${{ matrix.os }}
          path: |
            evaluation/results.json
            traces.jsonl
